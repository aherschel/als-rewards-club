type TacoRecipe @model @auth(rules: [{ allow: owner }, { allow: public, provider: iam, operations: [read] }]) {
  title: String!
  description: String!
}

type Query {
  generateTacoRecipe(prompt: String!): String! @aws_cognito_user_pools
}
